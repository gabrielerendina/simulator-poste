apiVersion: apps/v1
kind: Deployment
metadata:
  name: simulator-poste-frontend
  namespace: simulator-poste
  labels:
    app: simulator-poste-frontend
    component: web
spec:
  replicas: 2
  selector:
    matchLabels:
      app: simulator-poste-frontend
  template:
    metadata:
      labels:
        app: simulator-poste-frontend
        component: web
      annotations:
        sidecar.istio.io/inject: "true"
    spec:
      containers:
      - name: frontend
        image: ghcr.io/gabrielerendina/simulator-poste-frontend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
          name: http
          protocol: TCP
        resources:
          requests:
            cpu: 50m
            memory: 128Mi
          limits:
            cpu: 200m
            memory: 256Mi
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 5
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 10
          failureThreshold: 5
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          capabilities:
            drop:
            - ALL
